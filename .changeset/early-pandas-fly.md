---
"@smoothtml/vite-plugin-sri": patch
---

Correct the README status note that incorrectly said SRI injection was not yet implemented. The plugin injects integrity hashes during `vite build`, as it has since 0.1.0.
